{
  "name": "brook",
  "version": "0.1.0",
  "description": "Fleet overwatch — a sleeping guardian for Rob's AI fleet",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "^0.0.50"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20250312.0"
  }
}
